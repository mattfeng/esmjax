sync:
  defaults:
    mode: two-way-resolved
    flushOnCreate: true
    ignore:
      vcs: true
      paths:
        - "mutagen.yml"
        - "mutagen.yml.lock"
        - "logs/"
        - "wandb/"
        - "*.pkl"
        - "*.msgpack"
        - "*.h5"
        - "data/"

  engaging:
    alpha: .
    beta: "txe1-login2.mit.edu:~/inductive/esmjax/"