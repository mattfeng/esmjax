sync:
  defaults:
    mode: two-way-resolved
    flushOnCreate: true
    ignore:
      vcs: true
      paths:
        - "mutagen.yml"
        - "mutagen.yml.lock"
        - "logs/"
        - "wandb/"

  engaging:
    alpha: .
    beta: "txe1-login2.mit.edu:~/inductive/esmjax/"
